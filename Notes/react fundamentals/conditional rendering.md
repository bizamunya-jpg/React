# Conditional rendering
- conditional rendering basically means: only do /show this if something is true
- for example " if marks are 5 or more, show "Passed!".
- in react, one simple way to do this is with an if statement
if (mark >= 5) {
    //student passed
}
